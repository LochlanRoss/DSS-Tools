# Feature request checklist

Working scratchpad: check items off when they are done.

- [x] Make this list into a checklist and check items off as they are completed.
- [x] Automatic updates and version control: check GitHub for releases, download on unmetered Wi‑Fi, offer to install when the installer is ready.
- [x] Make the workbook / cache hash more robust against non–DSS metadata churn (semantic hash over preferred dated sheets and `K25:AZ36` only).
- [x] Click and drag columns in any viewer to reorder them.
- [x] Per-column header filtering (checkbox list with check all / uncheck all) without losing one-click header sort (sort ascending/descending in the same header menu).
- [x] Persist column visibility, widths, sort state, column order, and per-column header filter selections across launches.
- [x] Error Report: add a button to check name typos manually.
- [x] Name typos popup: note that the warning can be enabled or disabled in settings.
- [x] Add a generalized UI standardization document for future agent handoffs on other projects (`UI Standardization Notes.md`).
- [x] Summaries tab: add **Daily by PF#**; rename weekly view to **Weekly by PF#**; rename combined weekly view to **Combined Summary by Week**; add **Combined Summary by Day**.

- [x] Check cell **AZ2** against the sheet name revision indicator; register a sheet parse warning when they disagree.

- [x] Sheet parse warnings use the same alert styling as error report rows.

- [x] When sheet parse warnings or error-report items are present, colour the relevant report sub-tabs and the parent **Reports** tab; clear when updated data no longer has those issues.

- [x] Verify revision parsing for many suffix forms (`rev 1`, `r1`, `r 1`, `r-1`, `r.1`, `Revision 1`, …) and rev-up logic for revisions greater than one.

- [x] Double-click **source file** in any supported list to open that workbook.

- [x] Column reorder drag with a visible drop indicator; order persists with other layout settings and resets with applicable defaults.

- [x] Per-sheet hashing plus combined file digest for faster refresh; **Update View** / reload can **partially** refresh only changed dated sheets when the rest of the cache still matches.

- [x] **Quick load** last opened DSS set on startup (optional), progress + cancel, hint near the progress bar, setting to disable, and a **configurable cancel hotkey** (presets + key-capture dialog; persisted).

- [x] Progress text while processing sheets: include **PF#-#** (from the filename) with the sheet name, not the full file name.

- [x] Prefer **newer DSS files first** by modified time when loading multiple sources; early partial UI preview while older workbooks continue loading (existing preview path).

- [x] **Summaries** tab order: daily tabs before weekly (`Daily by PF#`, `Weekly by PF#`, combined day, combined week).

- [x] Auto-size column widths from header + cell text; cap **Source File** / `source_file` and other path-like columns (`sources`, `*_path`) so long filenames do not blow out the layout; other columns cap at a large pixel ceiling for sanity.

- [x] Column resize: **no column uses `stretch`**, so resizing one column does not shrink the next; total table width follows the sum of column widths and the horizontal scrollbar appears when the table is wider than the view.

- [x] **Word wrap (off) / (on)** toggle per `DataTable` (toolbar); wraps cell text to column width with newlines, increases Treeview row height via a per-table style so wrapped lines are visible; double-click source strips newlines before opening.

- [x] When I click the clear cached DSS button, it does not clear memory hits, it only clears stored data. Can you dump the memory as well?

- [x] I want to merge the funcitonality of the open DSS workbooks and add DSSs buttons. Currenly the open DSS workbooks clears whats open and opens new ones. Add DSSs adds to the existing open books. Tests with users have indicated this is unclear. I want to effectively only have the Add DSSs button so this only ever adds workbooks, and simply count on the remove DSSs button to clear when the user wants to

- [x] Right now when an error exists in either the error report or sheet parse warnings tabs, an exclamation point appears in their tab names, as well as their parent, and only the window around them changes the colour of its borders. I don't want to change the colour of this border. I want the tabs themselves (which are currently grey) to change to the same colour red as the error report listing red. 

- [x] I would like the cancel button aligned with the progress bar. Right now it is centered vertically between the progress bar and the hint below it. The hint below the progress bar now pushes the rest of the UI further down and leaves dead space, which I do not like. I would like to move this hint left of the progress bar and reclaim the space below the progress bar for the windows below. 

- [x] Please move the pick buttons closer to the colour codes. It is difficult to track across the screen visually and associate the correct pick button with the desired label.

- [x] Please add the app version number to the configuration page.

- [x] Please add colour configuration for the top bar section of the main window (the top buttons down to just above the summaries and raw tabs, progress bar included), by default make this a very dark grey that is near black, change the font to be a white readable on this black. Please make the colour of table background colours modifiable, same with the window colour around the tables. Make these by default a just off-white grey and white respectively by default.

- [x] Prefer `@jatechpowersystems.com` when Outlook name resolution finds multiple matching addresses for the same exact display name.

- [x] Add stricter fuzzy address-book typo detection for likely same-person spelling errors (for example `Dwayne Moffat` vs `Dwayne Moffatt`) without flagging different people who merely share a first name.

- [x] Add an email-missing suppression feature so intentionally missing employee emails are not highlighted or re-queried during Outlook sync.


- [x] Change the highlight behaviour of cells in tables to change the colour of the cell to blue when selected. Currently, there is only a small blue line that appears on the left of the cell. I would like the whole cell to change colour.

- [x] Make the cells highlightable and copyable. I would like to be able to copy and paste information out of these tables directly into other programs like microsoft word and emails in outlook.

- [x] In the reports section, when errors occur they generally cite specific cells in which the errors or suspected errors occur. Can we make it so any instance of an error report allows the user to double click the entry and open the source sheet with the excel cursor selected over the offending cells? Open it on the right worksheet, with the selection covering the offending cell.


- [x] The PF# extraction should include the dashed phase number if applicable.

- [x] Summary by employee should have by default the most recent week, but also include an option to change to older weeks or week ranges, enable a range and have a this week button, and a last week button

- [x] Add a checkbox to the warnings page and a suppression checkbox on each entry. 

- [x] Add a button to refresh the emails page that will work during contact list syncing, or better yet, update the employee page each time a new email is added that is on the employee list. (plus a cooldown, no more than 1Hz)

- [x] Add telemetry to contact list syncing that gives progress

- [x] Add ability to suppress multiple entries at once with a button or right click

- [x] Add name typo employee merge. Right click a typod name to merge with another name. If merged, always display the correct name unless calling out the typo in a report

- [x] Employee list does not appear to be updating as the contact list changes. 

- [x] 10 minutes in and the contact sync still displays working. Previous request for progress and telemetry would be nice. Perhaps expand the syncing outlook emails status message to include the latest synced email.

- [x] Ensure contact sync doesn't wait endlessly on name typos

- [x] Consolidate all warning/problem pages into a single `Data Review` page while keeping drill-through to workbook, sheet, and cell/range context.
- [x] Add a unified warning review surface with per-row suppression state, inline merge actions for typo cases, and a default-hidden `Show Suppressed` toggle.
- [x] Reorganize `Settings` into clearer sections for `General`, `Loading`, `Warnings`, `Appearance`, `Persistence`, and `Diagnostics`.
- [x] Split update-check timing from DSS hash-poll timing so automatic update checks can be tuned independently.
- [x] Add explicit persistence/retention settings visibility for suppressions, hidden employees, merged names, and saved manual email addresses.
- [x] Add review-state indicators showing when suppressions, hidden employees, or name merges are active.
- [x] Cache expensive review passes such as name-typo analysis against the currently loaded DSS set to avoid repeated long UI stalls.
- [x] Add performance controls for large DSS batches, including configurable parse parallelism and reduced live-refresh behavior while loading.
- [x] Expand appearance customization beyond individual colours into grouped surfaces such as window chrome, panels, tables, headers, selections, tabs, and button families.
- [x] Add appearance presets such as `Default Light`, `High Contrast Light`, and `Soft Neutral`, with one-click reset/apply behavior.
- [x] Improve top-level layout grouping so loading controls, filters, progress, and review actions are more visually distinct.
- [x] Improve the Employees page layout into clearer roster/details/actions sections with stronger visual separation.
- [x] Add a post-load summary/status area showing loaded files, employees, PF coverage, warnings count, and hidden/suppressed state at a glance.
- [x] There is no summary window for hours on the DSS total. I would like another summaries page which shows each DSS dash number hour total so far, as well as each overall DSS with all the dash numbers added up together. I would like to see every listed DSS's total hours so far. This should include ST, OT, DT, and Expanded Hours. This should be by PF and not by employee.
- [x] The UI does not actually refresh when the refresh button is pressed. The user has to make changes in the UI to selections in order for it to refresh.
- [x] Make the name typo check occur after any DSS load, but not after a zero-diff refresh, and run it as a separate background step so it does not continue hanging the UI while the DSSs load.
