# Feature request checklist

Working scratchpad: check items off when they are done.

- [x] Make this list into a checklist and check items off as they are completed.
- [x] Automatic updates and version control: check GitHub for releases, download on unmetered Wi‑Fi, offer to install when the installer is ready.
- [x] Make the workbook / cache hash more robust against non–DSS metadata churn (semantic hash over preferred dated sheets and `K25:AZ36` only).
- [x] Click and drag columns in any viewer to reorder them.
- [x] Per-column header filtering (checkbox list with check all / uncheck all) without losing one-click header sort (sort ascending/descending in the same header menu).
- [x] Persist column visibility, widths, sort state, column order, and per-column header filter selections across launches.
- [x] Error Report: add a button to check name typos manually.
- [x] Name typos popup: note that the warning can be enabled or disabled in settings.
- [x] Add a generalized UI standardization document for future agent handoffs on other projects (`UI Standardization Notes.md`).
- [x] Summaries tab: add **Daily by PF#**; rename weekly view to **Weekly by PF#**; rename combined weekly view to **Combined Summary by Week**; add **Combined Summary by Day**.

-I want to check a new cell for errors: I want to register a sheet parsing error whenever a specific cell which indicates whether a DSS is a rev or not does not match the sheet name. That cell address is the same for every file and every sheet, its address is: AZ2

-Whenever a sheet parse error is registered, I want the sheet parse error to show up in the list in the same font colour/highlight colour as the error reports do. 

-Whenever a sheet parse error or an error report error is logged, I want the tab for that page to turn the same colour of formatting (either the error report tab or the sheet parse warnings tab). Whenever either of those tabs turn red, I want the parent tab to turn red (the whole reports upper tab). I want them to stay red until cleared. By cleared I mean the data is updated and no longer has the problem in it.

-I want to verify that the rev up ignore previous rev logic works for revs greater than one, and also works for different permutations and combinations of "rev 1" " r1" "r 1" "r-1" "r.1" "Revision 1" and so on. 

-Whenever I doubleclick a source file in any list, I would like to open that source file.

-I want to be able to click and drag any column in any list to re-order the columns. I want the column order to persist as though it was a user setting and reset whenever a reset to default that is applicable to it resets. I want to be able to either A) see the column live as I drag it or B) see some kind of indicator for which two columns the dragged will land between. 

-Can the hashing logic be updated to improve the speed of refreshing? Right is it accurate to say we only hash the whole file's sum of each sheet's area of interest? If so, can we continue to hash the whole file but also now store the sheet hash? If we do that we can change the behaviour of the update view button to only reload specific sheets that change instead of the whole workbook/file. 