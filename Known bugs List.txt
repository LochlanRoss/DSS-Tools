Delete entries from this list after you ask me directly if they are fixed and I confirm they are. 

There is a bug with the revision counting. Some sheets are not showing as revs when they really are. For example: I have a sheet named "2026-04-26 rv1" whose AZ2 signature matches at "1", but the parser flags this entry as a parsing error and claims "AZ2 indicates a revision sheet, but the sheet name has no a revision suffix (parsed revision 0). (AZ2 raw value: '1')" Can we expand the logic to look for any "r" case insensitive character and any number and assign that automatically as a rev, then compare that number against the AZ2 signature? — **Fix attempted:** `REVISION_PATTERN` now treats `rv` + digits (e.g. `rv1`, `RV2`) like other revision suffixes before the generic `r` + digits branch.


The create bug report button does not work and is bugged. "Could not create the bug repott draft. (-2147352567, 'Exception occurred,•, (4096, 'Microsoft Outlook', 'Path does not exist. Verify the path is correct.•, None, O, -2147024893), None)"